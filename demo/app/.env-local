# ── Algorand MPP Demo App Configuration ──
# Vite exposes these as import.meta.env.VITE_*

# Algod URL for balance queries and transaction params
VITE_ALGOD_URL=https://testnet-api.4160.nodely.dev

# Demo server API base URL (proxied in dev via vite.config.ts)
VITE_API_BASE_URL=http://localhost:3000

# Vite dev server port
VITE_PORT=5173
